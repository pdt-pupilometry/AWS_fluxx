# Dependencias opcionales para desarrollo local (mismo pin que las Lambdas).
# NO es lo que se despliega a AWS -- eso lo define cada
# functions/*/requirements.txt (usado por sam build).
#
# IMPORTANTE: usa Python 3.12 (la misma version que corre en Lambda);
# numpy==1.26.4 y onnxruntime==1.19.2 no tienen wheels para Python 3.13+.
#
# Uso:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Si no tienes python3.12 instalado: `brew install python@3.12` (macOS).

opencv-python-headless==4.10.0.84
numpy==1.26.4
onnxruntime==1.19.2
requests==2.32.3
boto3==1.35.99
