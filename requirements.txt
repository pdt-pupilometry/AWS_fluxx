# Dependencias para desarrollo local: correr los scripts de testing/ y los
# tests de pytest. NO es lo que se despliega a Lambda -- eso lo define cada
# functions/*/requirements.txt (usado por sam build al armar cada imagen).
#
# IMPORTANTE: usa Python 3.12 (la misma version que corre en Lambda), no la
# version mas nueva que tengas de sistema -- numpy==1.26.4 y
# onnxruntime==1.19.2 no tienen wheels para Python 3.13+.
#
# Uso:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Si no tienes python3.12 instalado: `brew install python@3.12` (macOS).

# --- Testing ----------------------------------------------------------------
pytest==8.3.4

# --- Mismas versiones que usan las imagenes Lambda (functions/*/requirements.txt),
# para que los tests corran contra el mismo comportamiento que en AWS --------
opencv-python-headless==4.10.0.84
numpy==1.26.4
onnxruntime==1.19.2
requests==2.32.3
boto3==1.35.99

# --- Solo para scripts/export_model.py (opcional, pesado: instala PyTorch) --
# No hace falta para correr los tests ni testing/local_test_inference.py con
# un .onnx ya exportado -- solo si vas a exportar un .pt entrenado por tu cuenta.
# YOLO26 necesita ultralytics>=8.4 (trae Segment26); 8.3.x no puede cargar el .pt
ultralytics>=8.4.0
onnx>=1.17.0
x